# xtodo
Todo and note application using the terminal user interface (TUI) written in Rust. Simple, nice-looking, efficient, and intuitive.


## Build

In suitable directory, run
```
git clone https://github.com/mabognar/xtodo
cd xtodo
cargo install --path .
xtop
```
Alternatively, run
```
cargo install xtodo
```

